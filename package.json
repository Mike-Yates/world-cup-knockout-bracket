{
  "name": "yates-cup",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "npm run generate:data",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "npm run generate:data",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest",
    "auto:update": "tsx ops/auto-update/run-if-result-due.ts",
    "generate:data": "tsx scripts/generate-participants.ts",
    "update:results": "tsx scripts/update-results.ts",
    "update:elo": "tsx scripts/update-team-elo.ts"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
